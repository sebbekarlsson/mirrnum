from setuptools import setup, find_packages


setup(
    name='mirrnum',
    version='1.0.0',
    install_requires=[
        'matplotlib'
    ],
    packages=find_packages(),
    entry_points={
        'console_scripts': [
        ]
    }
)
